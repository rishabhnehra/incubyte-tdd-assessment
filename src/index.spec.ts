import { describe, expect, it } from "vitest";

describe("String calculator", () => {
  it.todo("should return zero on empty input string");
  it.todo("should return the same value as input if no other number");
  it.todo(
    "should return sum of all the numbers in input string (comma separated)"
  );
});
